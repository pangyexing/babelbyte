# AI Model Configuration
# Most settings are now configured via environment variables in .env
# See .env.example for: CLAUDE_MODEL_*, CODEX_MODEL_*, MODEL_TIER_*, QUALITY_*

# Task-level model overrides (optional)
# Uncomment to override model for specific task types
task_overrides:
  # event_confirm: haiku
  # event_title: haiku
  # report: opus
